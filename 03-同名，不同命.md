# 同名，不同命

> 两个人都叫FDE，名片上的英文一模一样。但他们的工作，像是两个平行世界。

## Kevin 的星期一

Kevin 的早上从 Slack 消息开始。

客户的 CTO 在频道里 @ 了他：「昨晚 API 调用延迟又高了，你能看看吗？」

他打开笔记本，登上客户的 AWS 账号，拉 CloudWatch 日志，过滤异常请求，找到是某个向量检索接口在高并发时超时。他在客户的环境里直接改了批处理参数，重新部署，延迟从 1.8 秒降到 0.3 秒。

下午 Zoom 会议，他给客户的产品团队演示改动效果，顺便聊了一下下周要做的 RAG 系统优化。客户全程用英文，会议结束后在 Slack 里发了个 🎉。

Kevin 所在的客户是旧金山一家 300 人的医疗 SaaS 公司，数据在 AWS，工具用 GitHub、Notion、Linear。Kevin 每周大约花 70% 时间写代码，20% 时间和客户沟通，10% 时间写内部文档。

这是硅谷 FDE 普通的一天。
---

## 林浩的星期一

林浩的早上从安检开始。

早上 8 点 50 分，他到了银行总部大楼门口。把手机交给安保人员存进柜子——进内网区不允许携带手机。换了一台行方配发的笔记本，设置了自己的账户密码，他已经把这个密码背得很熟了。

会议室里，行方 IT 的人已经到了。今天的任务是解决一个推理延迟问题——行方自己的 A100 服务器上跑的 vLLM 实例，在并发超过 50 时响应时间从 1.2 秒跳到 8 秒。

GitHub 上不去。Google 上不去。Stack Overflow 上不去。

林浩打开本地缓存的 vLLM 文档，开始排查 KV Cache 的配置。

下午三点，他找到了原因：行方的 GPU 服务器内存分配策略没有针对批处理推理优化，把 `gpu_memory_utilization` 从 0.85 调到 0.92，延迟降了下来。

他把修改记录整理成一份内部文档，发给字节内部的产品团队，标注这是「私有化部署场景下的常见配置陷阱」。

晚上，行方请他吃饭。项目进展顺利，客户关系维护。他八点才到家。
---

## 五个维度，两种处境

Kevin 和林浩做的是同一个岗位，但撑起这个岗位的底层逻辑完全不同。

### 数据环境

Kevin 两天内拿到了客户的 AWS 访问权限，通过 IAM 角色控制数据范围，全程在云端操作。

林浩申请把一个 10GB 的测试数据集从内网 A 区传到 B 区，走了三周的安全审批——信息安全部门、合规部门、IT 部门依次签字，最后才放行。

私有化部署是国内大客户的默认要求，不是谈判筹码。数据不出内网是硬性红线，合规周期是工期的一部分，不是例外情况。

### 核心技术挑战

Kevin 最头疼的问题：客户的系统能跑，但 adoption 率只有 30%。他花了大量时间做用户培训、改 UI 流程、把 AI 嵌进客户现有的工作流里。

林浩最头疼的问题是 vLLM 的推理速度和 GPU 资源调度。在没有外网的环境里，遇到 bug 只能靠本地文档和自己的经验。技术的底层越往下，越没有现成答案。

### 客户结构

Kevin 的客户 CEO 在 Slack 里能直接拍板，一个功能想法上午提，下午就能开始排期。

林浩要推动一个字段格式的修改，需要过业务部门、IT 部门、合规部门、信息安全部门，最快的一次走了三个月。国企和大型金融机构的决策链，是林浩工作里最消耗时间的部分，但也是他最核心的能力壁垒——懂这套流程本身就是一种稀缺技能。

### 面试逻辑

Palantir 的 FDE 面试有专门的 Decomp 轮：给你一个模糊的业务问题，考察你如何拆解、量化、提出假设并验证。这是硅谷 FDE 面试的核心能力测验。

国内面试更直接：技术栈深度 + 完整交付案例。知乎上的面经总结说，编程题写对只值 60 分，剩下 40 分在于你能不能边写边解释自己的思路——因为 FDE 在客户现场就是这么工作的。
### 薪资结构

Kevin：中位数薪资 $173K，期权占大头，顶端 $630K+。

林浩：35K-70K/月，基础工资比例高，约45-105万/年，期权比例相对低，但国内大厂的 RSU 流动性越来越好。

| 维度 | Kevin（硅谷） | 林浩（国内） |
|---|---|---|
| 数据环境 | AWS云端，API调用 | 私有化，数据不出内网 |
| 核心挑战 | 客户adoption、ROI量化 | vLLM/K8s/GPU基础设施 |
| 客户结构 | 中型科技/SaaS公司 | 大型国企/金融/制造 |
| 面试重点 | Decomp轮、客户模拟 | 技术栈深度+交付案例 |
| 薪资范围 | $173K-$630K/年 | 35K-70K/月（约45-105万） |
---

## 陈默圈出了哪一列

陈默把这张表对照自己过了一遍。

没有 AWS 账号，但熟悉内网环境，做过几个银行系统的对接项目。英文能看文档，但没有外国客户经历。对 vLLM 没经验，但 K8s 用过。

他把「林浩那列」用红笔圈起来，接着往下翻。

路选对了，下一个问题是：国内这条路，具体需要什么。

---

> **深度阅读**：
> - [腾讯云FDE JD全文](https://cloud.tencent.com/developer/article/2674646)
> - [字节火山引擎FDE JD](https://www.quanzhi.com/job/6a2b23a61433e769ad700020)
>
> **下一章**：[第4章——三份JD，三种逻辑](04-三份JD，三种逻辑.md)
>
> **关注「AI办公前线」** 获取本章配套文章：《同样叫FDE，字节的和Palantir的完全不是一回事》
