# 陈默问：要干这些，我需要什么技能

> 陈默把林浩那一天的时间表打印出来，对着自己的简历看了很久。

陈默做了五年Java后端。REST API写过几百个，数据库优化做过，上线流程走过，K8s用过但没深入过。

他把那份简历和林浩那一天的工作对照了一遍，发现有几段完全对不上：vLLM是什么？RAG链路他知道概念但没搭过生产级的。Prompt工程？他以为就是把问题问清楚。

他在纸上写下一个问题：**要干这些，我具体需要什么技能？**

然后他开始一层一层往下找答案。

---

## 第一层：底层工程能力

这一层是硬门槛。没有这层，后面的都是空话。

### Python / TypeScript 生产级代码

重点是「生产级」三个字。

会写Python分析脚本和能写上生产环境跑的代码，差距不是语法，是工程习惯：错误处理、日志埋点、配置管理、单元测试。林浩在银行内网调试vLLM的时候，排查问题靠的是清晰的日志，不是反复print。

TypeScript在现代AI应用栈里的位置越来越重要。字节的内部工具链大量用TypeScript，MCP协议的官方SDK也是TypeScript先行。

陈默的Java功底是资产，但要做FDE，Python得到生产级水准。

### 容器和私有化部署基础

国内FDE要会的不是Kubernetes的所有细节，是能在客户环境里独立把AI服务跑起来。

最小知识集：Docker镜像构建、docker-compose本地部署、K8s基础资源（Deployment、Service、PVC）、能看懂客户的集群配置、能排查Pod启动失败。

林浩在苏州那次，客户的服务器从来没跑过深度学习任务，他两天半独立搞定，靠的就是这层能力。

### 网络和权限基础

进了金融/制造客户的内网，你要能理解：为什么这个API调用超时（可能是防火墙白名单）、为什么模型输出到不了前端（反向代理配置）、为什么内网A区的数据传不到B区（VLAN隔离）。

这些不是FDE的专业技能，是FDE在内网环境独立生存的基础。

---

## 第二层：AI工程核心能力

这一层是国内FDE岗位的真正门槛，也是薪资差距的来源。

### RAG管线工程

检索增强生成（RAG）是国内企业AI落地最常见的技术路径之一。银行要建知识库、制造业要建设备手册检索系统、政务要建政策问答机器人——绝大多数都是RAG的变体。

能力要求不是「会调LlamaIndex API」，是：

- 能设计文档切分策略（chunk size、overlap、父子chunk）
- 能调选向量模型（embedding model选型）和向量数据库（Milvus、Chroma、PGVector）
- 能做检索质量评估（召回率、精确率、端到端问答准确率）
- 能排查检索结果差的原因（是文档切分问题？还是向量模型问题？还是reranker没调好？）

这件事字节、腾讯、智谱的JD里都写了，不是加分项，是基础项。

### Agent编排

2024年之前，大部分AI应用是「用户输入→模型输出」的单轮模式。2025年之后，越来越多的企业需求是多步骤的：先查数据库，再调内部API，再生成报告，最后发通知。

这就是Agent工作流。

LangGraph是目前国内FDE项目里用得最多的编排框架，它的有向图模型适合表达复杂的工作流（带条件分支、循环、人工干预节点）。此外还要了解CrewAI（多智能体协作）和AutoGen（微软推的框架）。

但更重要的能力是：**知道什么时候不用Agent**。Agent工作流越复杂，出错点越多，在客户的生产环境里越难维护。林浩会的第一件事，是帮客户判断这个需求是不是真的需要Agent，还是一个精心设计的单次调用就能解决。

### Prompt工程和Evals

Prompt工程不是「把问题写清楚」那么简单。

国内FDE场景的Prompt工程包含：
- 系统提示词设计（角色定义、输出格式、边界约束）
- Few-shot样本选取（哪些示例最能代表任务分布）
- 链式思考引导（Chain-of-Thought在复杂推理任务的应用）
- 国内大模型的特殊性（文心一言、通义千问、豆包的Prompt习惯和GPT不完全一样）

Evals（评估）是更常被忽略但更重要的能力。FDE的工作是对落地结果负责，不是对代码提交负责。要量化落地效果，就需要Evals：设计测试集、跑自动化评估、对比不同Prompt版本的效果、把结论整理给客户看。

这是FDE和普通AI工程师最大的能力分野之一。

### MCP协议对接

MCP（Model Context Protocol）是Anthropic在2024年底推出的协议，把AI模型和外部工具、数据源的对接标准化了。腾讯云在国内大力推广，把企微、腾讯文档、内部数据库都做成了MCP服务器，FDE负责帮客户把这些打通。

会MCP意味着：理解Server/Client/Host三层架构、能写一个自定义MCP Server（把客户的内部API封装成工具）、能把MCP Server接入主流AI框架。

---

## 第三层：国内特有技能

这一层是硅谷FDE不需要、国内FDE必须有的。

### 私有化部署专项

私有化是国内大客户的默认要求。

核心技能集：
- **vLLM**：目前私有化部署大模型推理最主流的框架。会配置gpu_memory_utilization、tensor_parallel_size、max_model_len；会做并发测试；会调Batching策略让吞吐量更高。
- **TensorRT-LLM**：英伟达出品，在A100/H100上能把推理速度再压缩30-50%。金融、制造对延迟敏感的场景会用到。
- **离线安装**：在内网部署，pip/conda/apt都要能走离线镜像。这是基础操作，但很多工程师在内网环境第一次遇到的时候会卡很久。
- **GPU资源调度**：了解CUDA基础、显存占用估算、多卡并行配置。

### 合规和安全基础

进金融和政务客户的项目，会遇到各种合规要求：

- 数据分类分级（哪些数据不能出内网、哪些数据要加密存储）
- 安全评审流程（接口上线前要过什么审批、要准备什么材料）
- 等保/行业安规基础知识（不需要是专家，但要知道客户在说什么）

这些不是编码技能，但决定了你的项目能不能推进。林浩在银行那次，联调接口比计划早做完了三天，但安全评审要两周——这两周他提前算进了计划里，因为他知道这个流程。

### 内部决策链导航

国内大型客户（国企、大型金融机构、大型制造企业）的决策链比硅谷客户长得多。

一个字段格式要改，可能要过：业务部门→IT部门→合规部门→信息安全部门。

FDE需要的能力不是绕过这个流程，是理解这个流程、提前把它算进项目计划里、在每个节点找到对的人推进。林浩那份「关系维护」的晚饭，是这个能力的一部分。

---

## 陈默的清单

陈默把这三层能力整理成一张表，对照自己：

| 能力 | 状态 |
|------|------|
| Python生产级代码 | 会写，工程化不够 |
| 容器/K8s基础 | 用过，不熟 |
| RAG工程 | 知道概念，没搭过生产级 |
| Agent编排 | 没用过LangGraph |
| Prompt/Evals | 完全没做过 |
| MCP协议 | 不了解 |
| vLLM私有化 | 从没接触 |
| 合规基础 | 做过银行项目，有一点了解 |

他数了一下：第三列比他预期的还要长。

但他也发现了一件事：这些技能不是需要同时全部掌握的。RAG工程和Agent编排有先后关系，先把RAG搭通，才有Agent编排的用武之地。私有化部署可以在有实际项目之前通过开源模型练手。Evals是在第一个RAG项目完成之后才会真正用上的东西。

他在表格旁边写了一行字：**「有序，不是同步。」**

然后他开始找第一件可以立刻开始的事。

---

## 补充：从哪里开始练

陈默在知乎上找到一个在国内FDE岗位做了一年多的工程师写的帖子，里面有一句话：

「别去背技能树，找一个真实的业务场景，从头搭一遍RAG，哪里不会搭到哪里，搭完你就知道第一步该补什么了。」

他截了图，存到手机备忘录。

第二天，他在本地用LlamaIndex搭了一个RAG系统，数据是自己整理的一份产品文档，跑了第一个检索测试。

结果召回率很差。他开始查原因。

---

> 📖 **深度阅读**：
> - [字节火山引擎FDE JD技术要求全文](https://www.quanzhi.com/job/6a2b23a61433e769ad700020)
> - [腾讯云FDE技术认证体系](https://cloud.tencent.com/developer/article/2674646)
> - [知乎·国内FDE面试技术准备经验](https://zhuanlan.zhihu.com/p/2064003821395878693)
>
> 🔖 **下一章**：[第7章——踩坑录：八个真实坑，你会踩哪几个](07-踩坑录.md)
>
> **关注「AI办公前线」** 获取本章配套文章：《国内FDE技能树：我整理了三层能力，第三层最容易被忽略》
