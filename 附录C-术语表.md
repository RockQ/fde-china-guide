# 附录C：术语表

> 第一次读到陌生术语时，回这里查。按字母顺序排列，中文术语按拼音排列。

---

**A**

**Agent（AI智能体）**
能够感知环境、做出决策、执行行动的AI系统。在FDE场景里，通常指能够调用外部工具、访问数据库、执行多步骤任务的LLM应用。区别于简单的「问答型AI」。

**AutoGen**
微软开源的多智能体框架，支持多个AI Agent之间的协作和对话。适合需要多个专业Agent分工合作的场景。

---

**C**

**Chunking（文档切分）**
RAG系统中，将长文档切分为小片段（chunks）再进行向量化的过程。切分策略（大小、重叠量、切分方式）直接影响检索质量。

**CUDA**
NVIDIA的GPU并行计算平台，深度学习模型训练和推理的基础。FDE在私有化部署时经常需要配置CUDA环境。

---

**D**

**Decomp（问题拆解）**
Palantir FDE面试的核心考察环节，给出模糊的业务问题，考察候选人如何将其拆解为可量化、可验证的子问题。国内面试中不常见这个环节，但这种思维方式是FDE的核心能力之一。

---

**E**

**Embedding（向量化）**
将文本转换为高维向量的过程，向量之间的距离反映语义相似度。RAG系统的核心技术之一。常用的Embedding模型有text-embedding-ada-002（OpenAI）、bge-large-zh-v1.5（智源，适合中文）等。

**Evals（评估框架）**
对AI系统输出质量进行系统化测量的方法。在FDE工作中，用于量化RAG检索质量、Agent工作流准确率、Prompt版本对比效果。是对落地结果负责的必要工具。

---

**F**

**FDE（Forward Deployed Engineer，前线部署工程师）**
嵌入客户现场、写生产级代码、对落地结果负责、把现场发现反哺给产品团队的工程师角色。由Palantir在2003年系统化，2025年后在AI行业快速普及。

---

**G**

**GPU（图形处理器）**
大规模并行计算芯片，是AI模型训练和推理的核心硬件。FDE私有化部署场景常见的GPU型号：A100（数据中心高端）、H100（最新高端）、T4（经济型）、4090（消费级高性能）。

---

**K**

**K8s（Kubernetes）**
容器编排平台，用于管理大规模容器化应用。私有化部署大型AI系统时，通常需要K8s来管理服务的部署、扩缩容和容错。

**KV Cache（键值缓存）**
Transformer架构中的推理加速机制，缓存之前计算过的Key和Value向量，避免重复计算。vLLM的性能优化很大程度上依赖KV Cache的高效管理。

---

**L**

**LangChain**
最流行的LLM应用开发框架之一，提供RAG、Agent、工具调用等模块。因功能复杂被部分工程师认为学习曲线陡，有些项目直接用更轻量的框架。

**LangGraph**
LangChain推出的有向图编排框架，适合构建有状态的、复杂的Agent工作流。国内FDE项目中使用率高，支持循环、条件分支、人工干预节点。

**LlamaIndex**
另一个主流RAG开发框架，专注于数据索引和检索，通常比LangChain在RAG场景下更简洁。

**LLM（大语言模型）**
基于Transformer架构的大规模语言模型，如GPT-4、Claude、通义千问、文心一言等。FDE的核心工作是让LLM在客户的生产环境里真正跑起来。

---

**M**

**MCP（Model Context Protocol，模型上下文协议）**
Anthropic于2024年发布的开放协议，将AI模型与外部工具、数据源的对接标准化。分为MCP Server（提供工具/数据）、MCP Client（AI应用）、MCP Host（运行环境）三层架构。

**Milvus**
开源向量数据库，国内私有化部署场景使用率高，支持亿级向量检索。

---

**P**

**POC（Proof of Concept，概念验证）**
在正式项目启动前，用于验证技术可行性的小规模实验。FDE踩坑录中的「坑一」是把POC当成方案设计——POC通过不等于生产级方案已定。

**Prompt工程**
设计和优化输入给LLM的指令（Prompt），以获得更好输出的技术。包括系统提示词设计、Few-shot示例选取、链式思考引导等方法。

**Private Deployment（私有化部署）**
将AI模型和服务部署在客户自己的服务器或私有云上，数据不经过外部网络。国内大型客户（金融、政务、军工）的默认要求。

---

**R**

**RAG（Retrieval-Augmented Generation，检索增强生成）**
将文档检索与LLM生成结合的技术架构。用户提问→检索相关文档→将文档加入LLM上下文→生成回答。适合需要基于特定知识库回答问题的场景。

**Reranker（重排序模型）**
RAG系统中，在初步检索之后对候选文档进行精细排序的模型。用于提升最终召回结果的相关性。

**RSU（Restricted Stock Unit，限制性股票单位）**
大厂薪资中的股权激励部分。字节等大厂的FDE岗位通常有RSU，随归属期释放。

---

**S**

**SA（Solution Architect，解决方案架构师）**
在售前阶段帮客户设计技术方案、推动合同签署的角色。SA的终点是合同，FDE的起点是合同。参见第1章的角色对比表。

---

**T**

**TensorRT-LLM**
NVIDIA推出的LLM推理优化库，在A100/H100等NVIDIA GPU上能将推理速度提升30-50%。适合对延迟要求高的生产场景。

**Tensor Parallelism（张量并行）**
将模型的权重矩阵分布在多个GPU上并行计算的技术。vLLM的`tensor_parallel_size`参数控制这个行为，多卡部署大模型时必须配置。

---

**V**

**Vector Database（向量数据库）**
专门存储和检索高维向量的数据库，RAG系统的核心组件。常见选项：Milvus（开源，适合私有化）、Chroma（轻量，适合开发阶段）、PGVector（PostgreSQL扩展，适合已有PG基础设施的客户）。

**vLLM**
开源LLM推理引擎，以PagedAttention技术大幅提升GPU内存利用率和推理吞吐量。国内私有化部署LLM的主流框架。参见第6章技能树。

---

**中文术语**

**等保（等级保护）**
中国网络安全等级保护制度，对信息系统的安全保护等级进行分级管理。金融、政务客户通常要求AI系统通过等保2.0三级认证。

**驻场**
工程师长期在客户现场工作的模式。国内FDE的核心工作形式，区别于偶尔拜访的传统技术支持。

**内网**
与外部互联网隔离的企业内部网络。金融、政务、军工客户的AI部署通常在内网环境，外网资源（GitHub、Google、Stack Overflow）无法访问。

**知识图谱**
以图结构存储实体和关系的知识表示方式。智谱AI等公司的FDE岗位会要求知识图谱相关能力，适合复杂关联查询场景。

---

> 🔖 **术语补充**：发现术语缺失或解释有误？在 [GitHub Issues](https://github.com/RockQ/fde-china-guide/issues) 提交反馈。
